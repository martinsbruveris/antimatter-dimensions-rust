---
status: Partial
feature: "6.1-6.5"
---

# Phase 6: Reality (Features 6.1–6.5)

**Status: implemented** (engine + save + UI for 6.1–6.5; the deferred pieces
are listed per feature below and in the decomposition doc). Corrections found
during implementation are folded into the text.

Analysis of the original's Reality layer and the port design for:

- 6.1 Reality Prestige
- 6.2 Glyphs
- 6.3 Perks
- 6.4 Reality Upgrades
- 6.5 Black Holes

The Automator (6.6) and everything celestial (Phase 7) are out of scope. This
doc records the original's mechanics (file references throughout), the frontier
cuts, and the Rust design.

Original sources: `src/core/reality.js`, `src/core/machines.js`,
`src/core/glyphs/*`, `src/core/secret-formula/reality/*`, `src/core/perks.js`,
`src/core/reality-upgrades.js`, `src/core/black-hole.js`, `src/game.js`
(`gainedGlyphLevel`, `getGameSpeedupFactor`), `src/core/math.js` (RNG + cost
scaling helpers).

## Frontier decisions

The Reality feature set is deeply entangled with celestials. We port the layer
as it behaves for a player who has realitied but touched no celestial:

- **Glyph types:** the 5 basic types (power / infinity / replication / time /
  dilation) plus the one-off **companion** glyph. Effarig, reality, cursed and
  music glyphs, cosmetics, glyph alteration and alchemy/refining are celestial
  content — out.
- **Glyph filter / auto-processor:** Effarig-gated — out. Manual glyph choice
  only (plus the deterministic no-START pick).
- **Imaginary Machines + Imaginary Upgrades:** gated on the RM hardcap
  (`1e1000` RM); far past the frontier — out (save fields round-trip).
- **BH inversion / auto-pause modes:** inversion needs Imaginary Upgrade 24 /
  Ra; auto-pause is a QoL setting tied to it. We port pause/unpause (with the
  5 s acceleration ramp) and leave inversion/auto-pause out.
- **Perks:** the full 35-node tree is ported (purchasing + tree rules). Perks
  whose effect targets a system we don't have yet (TT autobuyer, EC
  auto-completion, dilation/ID/replicanti autobuyer speedups) get their state
  tracked and their effects marked *deferred* — same convention as Break
  Infinity's deferred effects.
- **Reality Upgrades:** all 25 ported (requirements, locks, effects). RU13's
  autobuyer improvements and RU25's Reality autobuyer were initially deferred;
  Automator Stage A (`2026-07-05-automator.md`) implemented the Eternity and
  Reality autobuyers, wiring RU13's mode unlock and RU25. RU13's TD / ×5-EP
  autobuyers remain deferred.
- **Speedrun / news / cosmetic extras:** out.

## Feature 6.1: Reality Prestige

### Unlock

The Reality study is **dilation study id 6** (`dilation-time-studies.js`):
cost 1 TT, requires TD8 study bought, `records.thisReality.maxEP ≥ 1e4000`,
and (first perk bought **or** all pre-Reality achievements unlocked).
`isRealityAvailable()` = `thisReality.maxEP.exponent >= 4000 &&
TimeStudy.reality.isBought`.

### Reality Machines (`machines.js`)

```
log10EP = log10(thisReality.maxEP + gainedEternityPoints())
if never realitied:  log10EP = min(log10EP, 8000); above 6000 the excess is ×0.25
rm = 1000^(log10EP / 4000 - 1)
if 1 ≤ rm < 10: rm = 27/4000 · log10EP − 26        (linear ramp to 10)
rm × realityMachineMultiplier (all sources celestial/shop → 1 in frontier,
                               except Achievement 167 ×2 — deferred with achievements)
floor, clamp at hardcap 1e1000
```

### Reality reward (`giveRealityRewards` / `processManualReality`)

- RM += gainedRM (simulated-reality amplification is celestial → multiplier 1).
- realities += 1, perkPoints += 1 (Achievement 154's binomial extra is
  deferred).
- Records updated (see below); recentRealities ring unshifted.
- **First reality:** locks in `seed = initialSeed`, grants the fixed *starting
  glyph* (power, strength 1.5, `powerpow` only) and the *companion glyph*
  (strength encodes pre-reality EP: `rarityToStrength(log10(EP)/1e6)`).
- **Later realities without the START perk:** generate 4 glyphs, pick index
  `permutationIndex(4, realities · ((initialSeed % 5) + 3))[0]`.
- **With START (firstPerk):** the player picks one of
  `choiceCount = max(1, firstPerk effect 4)` glyphs (UI modal).

### Reality reset (`finishProcessReality`)

Everything from the Eternity layer down resets, *plus* the Eternity-layer
things an Eternity keeps:

- bestReality.bestEP updated from final EP before the reset.
- `sacrificed = 0`; challenges re-initialized (with keepAutobuyers milestone
  the NCs come back completed — but note eternities are reset *after*, so the
  original passes `isReality: true` to skip that; effectively **no**
  milestone-keeps apply since eternities → 0... except RU10 bumps them to 100).
- Infinities + banked infinities → 0; best/this infinity records reset;
  dimboosts/galaxies → 0; `break` → false; IP-mult purchases → 0 (out of
  frontier), infinity power / time shards → 0; `Replicanti.reset(true)` (full:
  locked, amount 0, upgrades and galaxies cleared).
- EP → 0, epmultUpgrades → 0, eternities → 0, this/best eternity records
  reset, eternity upgrades cleared, `totalTickGained` → 0, EC completions
  cleared, unlockedEC/EC-current/requirementBits → 0, `respec` → false,
  eterc8 budgets reset.
- Time studies + TT cleared (`Currency.timeTheorems.reset()` respecs the
  tree and zeroes both the amount **and** `maxTheorem` plus the TT purchase
  counts), dilation studies
  cleared, `dilation.active` false, dilation upgrades + rebuyables cleared,
  TP/DT/TGs → 0, thisInfinity/thisEternity maxAM → 0, thisReality records
  reset, antimatter → starting amount, ADs/tickspeed reset, IDs full reset,
  TDs full reset (amount **and** purchases: `fullResetTimeDimensions`),
  autobuyer resets as on Eternity.
- Achievements: `lockAchievementsOnReality` — without the ACHNR perk all
  pre-Reality achievements (rows 1–13) re-lock and `achTimer` starts
  regranting them (below).
- Glyphs: equipped glyphs stay equipped (respec only if the respec checkbox
  was ticked).
- After reset: if RU10 bought, `applyRUPG10()` (complete all NCs, 4 boosts,
  1 galaxy, `break = true`, eternities bumped to 100, replicanti unlocked,
  maxed AD autobuyers); if RU14, eternities gain starts; perk `startAM` etc.
  bump currencies (we apply perk bumps in `starting_*` helpers).

### Records (`player.records`)

New modelled slices:

- `thisReality`: time, realTime, maxEP, maxReplicanti, maxDT,
  bestEternitiesPerMs (deferred: bestRSmin).
- `bestReality`: time, realTime, RMmin, glyphLevel, bestEP, glyphStrength
  (the `*Set` glyph-loadout snapshots are display-only → deferred).
- `recentRealities`: ring of 10 `[time, realTime, RM, realityCount, "", projIM]`
  → modelled as `{time_ms, real_time_ms, rm, reality_count}`.

`thisReality.maxEP/maxReplicanti/maxDT` are advanced in `tick`; they feed the
RM formula and glyph level.

### Requirement checks (`player.requirementChecks`)

RU6/7/8/9/10/24 and Achievement 137 etc. need a small slice:

- `eternity.noRG` (reset on eternity; cleared when buying an RG)
- `reality.noEternities`, `reality.noInfinities` (reset on reality)
- `reality.maxGlyphs` (peak equipped glyph count)
- `reality.gainedAutoAchievements` (set when auto-achievements grant while
  ACHNR not bought — actually set on reality when locking, or by the ACHNR
  purchase; cleared on reality: `player.reality.gainedAutoAchievements = false`)

### Auto-achievements (`reality.js` + `Achievements` timer)

After a reality (without ACHNR), pre-Reality achievements regrant in id order
every `30 − perk reductions` minutes (ACH1..4 → 20/12/6/2). `achTimer`
accumulates real time; when it crosses the period the next locked achievement
in rows 1–13 unlocks and the timer resets; `gainedAutoAchievements` set. The
`autoAchieve` toggle can suspend it.

## Feature 6.2: Glyphs

### Data model

```rust
pub struct Glyph {
    pub id: u32,             // unique, monotonic
    pub idx: u32,            // slot (inventory 0..119, active 0..slots-1)
    pub kind: GlyphType,     // Power/Infinity/Replication/Time/Dilation/Companion
    pub strength: f64,       // 1.0..=3.5 (rarity = (strength-1)*40)
    pub level: u32,
    pub raw_level: u32,
    pub effects: u32,        // bitmask, original bit indices (0..=19, companion 8|9)
}
pub struct GlyphState {
    pub active: Vec<Glyph>,
    pub inventory: Vec<Glyph>,
    pub sac: [f64; 5],       // power/infinity/time/replication/dilation
    pub protected_rows: u32, // default 2
}
```

Inventory: 120 slots, `protectedRows × 10` protected. Active slots:
`3 + RU9 + RU24`. Equipping is one-way mid-reality (unequip happens only via
the respec checkbox at the next reality — the glyph "undo" feature is out of
frontier since it needs... actually undo is base-game; **deferred** for now,
documented).

### RNG (`glyph-generator.js` + `math.js`)

Seeded xorshift32 on `i32` state:

```
state ^= state << 13; state ^= state >>> 17; state ^= state << 5
uniform() = state · 2.3283064365386963e-10 + 0.5   // state as signed i32
```

`normal()` = Marsaglia polar, with `secondGaussian` cached in the save
(default sentinel 1e6). Seed + secondGaussian persist (`reality.seed`,
`reality.secondGaussian`); `initialSeed` is rolled at save creation.
Determinism holds given the same save — matching the engine principles.

### Generation

- `randomStrength`: `gaussianBellCurve(rng) × strengthMultiplier(RU16 ×1.3)`,
  plus rarity boosts (Effarig/achievement 146/effarig-sacrifice — all out or
  deferred), then ceil to 1/400 steps, capped at 3.5. **Note:**
  `gaussianBellCurve` uses the cubic-polynomial approximation of
  `max(normal()+1,1)^0.65`.
- `randomNumberOfEffects(type, strength, level, rng)`: two uniforms drawn
  up-front; `num = min(4, floor(random1^(1 − √(level·strength)/100) · 1.5 + 1))`;
  RU17 gives `+1` with its probability using random2.
- `generateEffects(type, count, rng)`: one uniform per possible effect of the
  type, pad draws up to 7, force the primary effect (`timepow`/`infinitypow`/
  `powerpow` bits 0/12/16) to weight 2, take the top `count` by weight.
- `randomType(rng, typesSoFar)`: uniform over the 5 basic types, excluding the
  currently-most-common types in `typesSoFar`.
- **Uniformity** (`uniformGlyphs`): active while `realities ≤ 20`. Ported
  verbatim (permutation-indexed type/effect spreading, driven by
  `initialSeed`); requires `permutationIndex` (Lehmer decode) in Rust.
- `glyphUncommonGuarantee`: with firstPerk... (actually unconditional in
  `glyphList`): one uniform + rarity redraws ≥1.5 assigned to a random choice
  if none was uncommon.
- Choice generation always creates ≥4 glyphs (RNG stability), slices to the
  choice count, and `finalize()`s the RNG back into the save.

### Glyph level (`getGlyphLevelInputs`)

With default weights (25 each) the Effarig weighting is the identity, so:

```
epBase   = (max(1, log10(EPforLevel)))^0.5 × 0.016     EPforLevel = max(thisReality.maxEP, EP + pending gain when canEternity)
replBase = (max(1, log10(thisReality.maxReplicanti)))^replPow × 0.025,  replPow = 0.4 + replicationglyphlevel glyph effect
dtBase   = (max(1, log10(thisReality.maxDT)))^1.3 × 0.025
eterBase = RU18 effect (max(√(log10(eternities+1))·0.45, 1)), else 1
baseLevel = epBase·replBase·dtBase·eterBase (perkShop=1)
instability softcaps at 1000 (rate 500) then 4000 (rate 400)
+ static adders: realityUpgrades row count (rows of 5 all bought) + achievements (148/166 — deferred)
level = max(1, floor(scaledLevel)), rawLevel = floor(baseLevel)
```

### Effects and engine sites

The 20 generated effects, their formulas (level `l`, strength `s`, adjusted
level via reality-glyph boost = none in frontier) and where they apply:

| bit | id | effect | combiner | engine site |
|----|----|--------|----------|-------------|
| 0 | timepow | `1.01 + l^0.32·s^0.45/75` | addExponents | TD multiplier `^x` (time_dimensions) |
| 1 | timespeed | `1 + l^0.3·s^0.65/20` | multiply | `game_speed_factor` |
| 2 | timeetermult | `((s+3)·l)^0.9` | multiply | `gained_eternities` |
| 3 | timeEP | `(l·s)^3·100` | multiply | `total_ep_mult` |
| 4 | dilationDT | `(l·s)^1.5·2` (Decimal) | multiplyDecimal | `dilation_gain_per_second` |
| 5 | dilationgalaxyThreshold | `1 − l^0.17·s^0.35/100` | product w/ 0.4 softcap | `tachyon_galaxy_threshold_mult` |
| 6 | dilationTTgen | `(l·s)^0.5/10000` TT/s | add | tick TT stream |
| 7 | dilationpow | `1.1 + l^0.7·s^0.7/25` | addExponents | AD mult `^x` while dilated |
| 8 | replicationspeed | `l·s·3` (Decimal) | multiplyDecimal | replicanti speed |
| 9 | replicationpow | `1.1 + l^0.5·s/25` | addExponents | replicanti multiplier `^x` |
| 10 | replicationdtgain | `0.0003·l^0.3·s^0.65` | special product | DT gain × `max(1, log10(replicanti)·x)` |
| 11 | replicationglyphlevel | `((l^0.25·s^0.4)^0.5)/50` | add w/ softcap 0.1 | glyph-level repl exponent |
| 12 | infinitypow | `1.007 + l^0.21·s^0.4/75` | addExponents | ID multiplier `^x` |
| 13 | infinityrate | `l^0.2·s^0.4·0.04` | add | infinity-power conversion `^(7+x)` |
| 14 | infinityIP | `(l·(s+1))^6·10000` | multiply | `gained_infinity_points` × |
| 15 | infinityinfmult | `(l·s)^1.5·2` (Decimal) | multiplyDecimal | `gained_infinities` × |
| 16 | powerpow | `1.015 + l^0.2·s^0.4/75` | addExponents | AD multiplier `^x` |
| 17 | powermult | `(l·s·10)^(l·s·10)` (Decimal) | multiplyDecimal | AD multiplier × |
| 18 | powerdimboost | `(l·s)^0.5` | multiply | dim-boost power × |
| 19 | powerbuy10 | `1 + l·s/12` | addExponents | buy-10 multiplier `^x` |

Companion effects (bits 8/9 in the non-generated table) are display-only.

`addExponents` = `Σx + (1 − n)`; effects read equipped glyphs of matching
type. Effect values are computed on demand (≤5 glyphs, cheap).

### Sacrifice (`glyph-sacrifices.js`, gated on RU19)

Gain: `((min(level,10000)+10)^2.5 · (1 + max(level−10000,0)/100) · strength)`.
Totals per type (cap 1e100 for effects):

- power: distant-galaxy scaling delayed by `floor(750·(log10(sac+1)/100)^1.2)`
- infinity: ×`1 + log10(1 + sac^0.2/100)` on ID8 purchase multiplier
- time: ×`(1 + sac^0.2/100)^2` on TD8 purchase multiplier
- replication: RG distant scaling delayed by `floor(1500·(log10(sac+1)/100)^1.2)`
- dilation: TP gain ×`max(sac,1)^(0.32·(log10(sac+1)/100)^0.1)`

## Feature 6.3: Perks

35 perks, each 1 PP; a perk is buyable if it's `firstPerk` (id 0) or adjacent
to a bought perk (connection graph from `perkConnections`). State:
`BTreeSet<u8>`-equivalent bitmask (`u64` keyed by a compact index; save uses
the original ids). Effects in frontier:

- firstPerk: 4 glyph choices; removes achievement requirement from the
  Reality study.
- startAM (5e130), startIP1/2 (5e15/5e130), startEP1/2/3 (10/5000/5e9),
  startTP (10 TP after dilation unlock): applied on the relevant resets.
- antimatterNoReset: boosts/galaxies keep antimatter & dims/tickspeed/sacrifice.
- studyPassive: TS122 ×50 EP, TS142 ×1e50 IP, TS132 ×3 replicanti.
- autounlockEU1/EU2: EU rows 1/2 free/1e10-cheaper auto-purchase.
- autounlockDilation1/2/3: dilation upgrade rows free (row 2/3) + auto-buy TT gen.
- autounlockTD: TD5–8 auto-unlock when affordable.
- autounlockReality: auto-buys the Reality study when conditions met.
- bypassIDAntimatter: ID unlocks lose their antimatter requirement.
- bypassTGReset: rebuyable dilation upgrade 2 keeps DT.
- bypassECDilation / bypassEC1..3/5 Lock: study/dilation requirement waivers.
- studyActiveEP: TS121 maximized; studyIdleEP: TS131/141 start at 15 min.
- studyECRequirement: ECs need only TT; studyECBulk: bulk EC completions.
- achievementGroup1–4: achievement timer −10/−8/−6/−4 min; achievementGroup5:
  keep achievements on Reality.
- retroactiveTP1–4: ×1.5/2/2.5/3 TP on buying the 3rd dilation rebuyable.
- Deferred (no engine target yet): autocompleteEC1–3, autobuyerDilation,
  autobuyerFasterID/Replicanti/Dilation, ttBuySingle/ttFree/ttBuyMax,
  dilationAutobuyerBulk.

UI: the original renders a vis-network graph. We port a faithful static
layout using the `layoutPosList` fixed positions (default layout index 1,
"Grid (Default)") as an SVG — nodes colored per family, edges as lines,
matching colors/sizes from `PerksTab` styling.

## Feature 6.4: Reality Upgrades

State: `upgrade_bits: u32` (ids 6–25), `rebuyables: [u32; 5]` (ids 1–5),
`upg_reqs: u32` (requirement-unlocked bitmask), `req_lock: u32` (player locks).

Rebuyables (ids 1–5): cost `getHybridCostScaling(count, 1e30, initialCost,
costMult, costMult/10, 1e309, 1e3, ...)` — below 1e30 plain geometric
(`initialCost·costMult^count`), which is all the frontier reaches; effect
`base^count` with base 3/3/3/3/5 (DT gain, replicanti speed, eternities ×,
TP ×, infinities ×).

One-time upgrades 6–25: costs 15/15/15/15/15/50/50/50/50/50/1500×4 + 1500,
1e5×5. Each has `checkRequirement` fired on events (eternity/crunch/reality
resets, per-tick) that sets `upgReqs` bit; several have `hasFailed` + player
lock semantics. Effects in frontier:

- RU6 replicanti speed ×(1 + RG/50); RU7 infinities ×(1 + galaxies/30);
  RU8 TP ×√(achievement power); RU9 +1 glyph slot; RU10 start-of-reality
  package; RU11 passive infinity gen (10%/s of crunch gain); RU12 EP
  ×TT^log2(min(realities,1e4)); RU13 (autobuyers, deferred) ; RU14 eternities
  += realities/s; RU15 TP ×max(√log10(epMult)/9, 1); RU16 glyph strength
  ×1.3; RU17 +50% extra effect chance; RU18 eternity glyph-level factor;
  RU19 glyph sacrifice; RU20 second black hole (needs BH1 unlocked); RU21
  remote galaxy scaling → 1e5; RU22 TD mult from days in reality; RU23
  replicanti ×(15/best reality minutes, cap 180); RU24 +1 glyph slot; RU25
  reality autobuyer (deferred).

## Feature 6.5: Black Holes

State per hole (`player.blackHole[i]`): `intervalUpgrades`, `powerUpgrades`,
`durationUpgrades`, `phase`, `active`, `unlocked`, `activations`. Global:
`blackHolePause`, `blackHolePauseTime` (for the 5 s unpause ramp).

- Unlock BH1: 100 RM. BH2: RU20.
- Values: interval `3600/10^id × 0.8^n` (BH1 id 0), power `180/2^id × 1.35^n`,
  duration `(10 − 3·id) × 1.3^n`. Achievement multipliers 145/155/158 deferred
  with those achievements.
- Costs: hybrid scaling (linear start 1e30) — base 15/20/10 ×(1 or 1000 for
  BH2), mults 3.5/2/4.
- Phase update per real-time tick (`updatePhases` with the active-period
  cascade: BH2's phase only advances while BH1 is active); `isPermanent` at
  duty cycle ≥ 0.9999.
- Game speed: while active, `factor ×= power^unpauseAccelerationFactor` per
  active hole; combined with the `timespeed` glyph multiplier in
  `game_speed_factor` (which becomes Decimal-free f64; EC12 fixed 1/1000
  keeps priority).
- Pause: freezes phase updates; unpausing ramps power over 5 s.

## Save/load

All new state maps onto the original save shape (dto.rs additions):

- `player.reality.*`: realityMachines (Decimal string), glyphs
  {active, inventory, sac, protectedRows}, seed, initialSeed, secondGaussian,
  rebuyables {1..5}, upgradeBits, upgReqs, reqLock.reality, perks (array),
  perkPoints, respec, achTimer, gainedAutoAchievements, autoAchieve.
- `player.realities` (root), `player.partSimulatedReality` (round-trip only).
- Glyph DTO: `{id, idx, type, strength, level, rawLevel, effects}`.
- `player.records.thisReality/bestReality/recentRealities`.
- `player.requirementChecks` slice.
- `player.blackHole[2]`, `blackHolePause`, `blackHolePauseTime`.
- Unknown/celestial fields keep round-tripping through the DTO's passthrough.

## UI plan

New "Reality" tab group (visible once `isRealityAvailable()` or realities>0):

- **Glyphs** subtab: equipped slots + inventory grid + sacrifice, reality
  button area (`GlyphsTab.vue`, `GlyphInventory.vue`, `GlyphComponent.vue` —
  vendor `glyphs.css` styles already in the vendored stylesheets).
- **Upgrades** subtab (`RealityUpgradesTab.vue`): 5×5 grid.
- **Perks** subtab: SVG network with fixed layout.
- **Black Hole** subtab: unlock button, per-hole status + 3 upgrade buttons,
  pause button.
- Header: Reality button (`RealityButton.vue`) with RM/glyph-level preview;
  glyph choice modal on reality with the selection UI.
- Reality reminders in Statistics are out of scope for now.

## Implementation slices

1. **6.1 engine**: RealityState, records slices, RM formula, reality reset,
   requirement checks, auto-achievements, dilation study 6, save round-trip.
2. **6.2 engine**: GlyphRNG + generator + uniformity, level inputs, glyph
   state/equip, effect readers at all sites, sacrifice.
3. **6.1+6.2 UI**: Reality button + modal, Glyphs tab.
4. **6.3 engine + UI**: perks + tree tab.
5. **6.4 engine + UI**: reality upgrades + tab.
6. **6.5 engine + UI**: black holes + tab; game-speed integration.

Each slice lands with unit tests and a save/load round-trip test; commits per
slice.
